cc_prebuilt_binary {
    name: "plpath_utils_manaus",
    srcs: ["plpath_utils"],
    device_specific: true,
    recovery: true,
    // Isso garante que o sistema de build não tente modificar o binário
    strip: {
        none: true,
    },
}

